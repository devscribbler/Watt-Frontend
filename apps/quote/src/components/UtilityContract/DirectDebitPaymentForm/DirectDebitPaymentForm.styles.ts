import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles(() => ({
  root: {},
  authorized: {
    marginBottom: '1em',
  },
  signatureFont: {
    fontFamily: 'cursive',
    fontSize: '2em',
    fontWeight: 500,
  },
}))
