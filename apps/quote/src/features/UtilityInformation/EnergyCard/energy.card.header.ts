import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  root: {
    display: 'flex',
  },
  titleContainer: {
    display: 'flex',
    alignItems: 'center',
    flexGrow: 1,
  },
  supplierLogo: {
    flex: 1,
  },
})
